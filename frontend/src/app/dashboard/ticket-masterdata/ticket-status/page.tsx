import React from "react";
import TicketStatusClient from "./components/TicketStatusClient";

const page = () => {
  return (
    <div>
      <TicketStatusClient />
    </div>
  );
};

export default page;
